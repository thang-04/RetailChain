---
phase: 01-toast-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - User thấy Toast notification thay vì alert() khi tạo StockIn thành công
    - User thấy Toast notification thay vì alert() khi tạo Warehouse thành công
    - Toast component có thể tái sử dụng ở các page khác
  artifacts:
    - path: "RetailChainUi/src/components/ui/sonner.jsx"
      provides: "Toast component từ shadcn/ui Sonner"
    - path: "RetailChainUi/src/pages/StockIn/CreateStockIn.jsx"
      provides: "Thay alert() bằng toast    - path:()"
 "RetailChainUi/src/pages/Warehouse/CreateWarehouseModal.jsx"
      provides: "Thay alert() bằng toast()"
  key_links:
    - from: "CreateStockIn.jsx"
      to: "sonner.jsx"
      via: "import { toast } from 'sonner'"
    - from: "CreateWarehouseModal.jsx"
      to: "sonner.jsx"
      via: "import { toast } from 'sonner'"
---

<objective>
Triển khai Toast notifications sử dụng shadcn/ui Sonner. Thay thế alert() trong CreateStockIn.jsx và CreateWarehouseModal.jsx bằng Toast notifications với vị trí bottom-right, thời lượng 5 giây, hỗ trợ 4 loại (success, error, warning, info).
</objective>

<context>
@./.planning/phases/01-toast-notifications/01-CONTEXT.md

From .planning/codebase/CONVENTIONS.md:
- Sử dụng shadcn/ui components
- Sử dụng functional components với Arrow Functions
- Import paths: @/components/...

Files cần sửa:
- RetailChainUi/src/pages/StockIn/CreateStockIn.jsx (lines 104, 128)
- RetailChainUi/src/pages/Warehouse/CreateWarehouseModal.jsx (line 115)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cài đặt Sonner</name>
  <files>RetailChainUi/src/components/ui/sonner.jsx</files>
  <action>
1. Chạy lệnh cài đặt Sonner: npx shadcn@latest add sonner -y
2. Hoặc tạo file sonner.jsx thủ công nếu lệnh không hoạt động:
   - Import { Toaster } from 'sonner'
   - Tạo component Toaster với position="bottom-right"
   - Export component để sử dụng trong App.jsx
  </action>
  <verify>Kiểm tra file sonner.jsx tồn tại trong src/components/ui/</verify>
  <done>Sonner component đã được cài đặt và có thể import</done>
</task>

<task type="auto">
  <name>Task 2: Thêm Toaster vào App.jsx</name>
  <files>RetailChainUi/src/App.jsx</files>
  <action>
1. Import { Toaster } from '@/components/ui/sonner'
2. Thêm component <Toaster /> vào App component (sau Router)
3. Đảm bảo Toaster hiển thị ở vị trí bottom-right
  </action>
  <verify>Kiểm tra App.jsx có import và sử dụng Toaster component</verify>
  <done>Toaster component đã được thêm vào App.jsx</done>
</task>

<task type="auto">
  <name>Task 3: Thay alert() trong CreateStockIn.jsx</name>
  <files>RetailChainUi/src/pages/StockIn/CreateStockIn.jsx</files>
  <action>
1. Import { toast } from 'sonner' (thay vì comment useToast)
2. Line 104: Thay alert("Vui lòng điền đầy đủ...") bằng toast.error("Vui lòng điền đầy đủ...")
3. Line 128: Thay alert("Đã có lỗi xảy ra...") bằng toast.error("Đã có lỗi xảy ra...")
4. Sau khi navigate('/stock-in') (line 124): Thêm toast.success("Tạo phiếu nhập kho thành công!")
  </action>
  <verify>npm run lint không có lỗi liên quan đến alert</verify>
  <done>CreateStockIn.jsx sử dụng toast thay vì alert</done>
</task>

<task type="auto">
  <name>Task 4: Thay alert() trong CreateWarehouseModal.jsx</name>
  <files>RetailChainUi/src/pages/Warehouse/CreateWarehouseModal.jsx</files>
  <action>
1. Import { toast } from 'sonner'
2. Line 115: Thay alert("Lỗi: " + ...) bằng toast.error("Lỗi: " + ...)
3. Sau onSuccess() (line 112): Thêm toast.success("Tạo kho thành công!")
  </action>
  <verify>npm run lint không có lỗi liên quan đến alert</verify>
  <done>CreateWarehouseModal.jsx sử dụng toast thay vì alert</done>
</task>

</tasks>

<verification>
1. Kiểm tra cả 3 file không còn alert()
2. Toast hiển thị ở bottom-right
3. Toast tự động ẩn sau 5 giây
4. Sử dụng được ở các page khác (toast có thể import từ 'sonner')
</verification>

<success_criteria>
- alert() đã được thay thế hoàn toàn bằng toast() trong cả 2 files
- Sonner component được cài đặt đúng cách
- Toaster component được thêm vào App.jsx
- Toast hiển thị với vị trí bottom-right, duration 5 giây
</success_criteria>

<output>
After completion, create `.planning/phases/01-toast-notifications/01-01-SUMMARY.md`
</output>
