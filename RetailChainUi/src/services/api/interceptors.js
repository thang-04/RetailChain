import axiosClient from './axiosClient';

// Example interceptor setup if needed more complex logic separately
// For now, axiosClient.js handles basic interceptors.
// This file is reserved for future complex interceptor logic (e.g. token refresh)

export const setupInterceptors = (store) => {
    // Setup interceptors that might need access to Redux store or Context
};
